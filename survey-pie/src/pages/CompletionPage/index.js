import styled from 'styled-components';

function CompletionPage() {
  return <CompletionPageWrapper>CompetionPage</CompletionPageWrapper>;
}

const CompletionPageWrapper = styled.div`
  background: aqua;
  padding: 4em;
`;

export default CompletionPage;
